Modify the existing Shortcut named “RPC Worker Harness”. Do not create a second worker. Preserve every existing action, authentication step, request leasing step, Pushcut wake behavior, existing method handler, result completion path, and unknown-method behavior.

Goal: add exactly two small native RPC branches. Do not rewrite or simplify the rest of the worker.

METHOD 1 — shortcuts.control

When request.method is exactly:
shortcuts.control

Behavior:
1. Take request.params, preserving it as a Dictionary.
2. Convert request.params to JSON text if the Run Shortcut input requires text.
3. Use the native Run Shortcut action to run the already-installed Shortcut named exactly:
   shortcuts.control
4. Pass that JSON text as Shortcut Input.
5. Capture the actual output returned by shortcuts.control.
6. Feed that output into the worker's existing result-completion path for the current request ID.
7. Do not use a URL, Safari, a-Shell, clipboard, Open App, or UI automation.
8. If the child Shortcut errors, preserve the existing worker error behavior rather than fabricating success.

This is an explicit known-tool branch. Do not implement a generic arbitrary-name dispatcher as part of this patch.

METHOD 2 — shortcuts.open_url

When request.method is exactly:
shortcuts.open_url

Read:
request.params.url

Behavior:
1. If url is empty, return the worker's normal structured error with code missing_url.
2. Use Apple's first-party Open URLs action on that URL.
3. Do not use Safari, a-Shell, shell commands, clipboard, or Open App.
4. If execution resumes after Open URLs, return through the existing completion path with an equivalent of:
   {"opened":true}
5. Do not treat worker completion as proof that an imported Shortcut registered successfully; registration is verified by a later native inventory/run request.

ROUTING REQUIREMENTS

- Add both branches alongside the current explicit method branches.
- Existing handlers such as probe.echo, shell.exec.simple, shell.exec, shell.inspect, phone.info, harness.info, linux.exec, and linux.inspect must remain unchanged.
- Unknown methods must continue to return the existing tool_not_found/unknown-method result.
- Do not alter the worker token, Supabase endpoint, lease/completion logic, Pushcut notification automation, or current request schema.
- Do not add a-Shell to either branch.
- Do not move, rename, duplicate, or replace RPC Worker Harness.

LOCKED EXECUTION

Preserve/enable Allow Running When Locked for RPC Worker Harness.

FINAL SELF-CHECK

Before finishing, visually verify:
- method == shortcuts.control reaches native Run Shortcut -> shortcuts.control with request.params forwarded as JSON input;
- its child output reaches the same current request completion mechanism used by existing successful handlers;
- method == shortcuts.open_url reaches native Open URLs using request.params.url;
- no existing handler or transport action was removed or reordered in a behavior-changing way.

This is a surgical two-branch patch to the existing worker only.